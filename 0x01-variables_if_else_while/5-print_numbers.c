#include <stdio.h>
/**
 * main - Entry point
 * Return: success
 */
int main(void)
{
	/*initialise digits*/
	int digits = '0';

	for (digits = 0; digits <= 9; digits++)
	{
	printf("%d", digits);
	printf("\n");
	}

	return (0);
}
