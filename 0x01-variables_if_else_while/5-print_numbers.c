#include <stdio.h>
/**
 * main - Entry point
 * Return: success
 */
int main(void)
{
	/*initialise digits*/
	char digits = '0';

	for (digits == 0; digits <= 9; digits++)
	{
	putchar(digits);
	}

	return (0);
}
