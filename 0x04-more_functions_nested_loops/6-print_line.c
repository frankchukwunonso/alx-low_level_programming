#include "main.h"

/**
 * print_line - literally prints line
 *
 * @n: it is a parameter
 */
void print_line(int n)
{
	int line, d;

	d = n - 1
	for (line = 0; line <= d; line++)
	{
		if (n > 0)
		{
			_putchar('_');
		}
	}
	_putchar('\n');
}


