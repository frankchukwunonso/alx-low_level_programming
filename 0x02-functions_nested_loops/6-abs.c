#include "main.h"

/**
 * _abs - computes absolute value
 *
 * @a: it's a parameter
 *
 *Return: d or a
 */
int _abs(int a)
{
	int d;

	if (a < 0)
	{
		d =  a * -1;
	return (d);
	}
	else
	{
		return (a);
	}
}
